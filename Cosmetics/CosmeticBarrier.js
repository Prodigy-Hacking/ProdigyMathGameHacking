// Gives you a transparent blue barrier whenever you are attacked, like the one the titan has.
Phaser.GAMES[0].prodigy.player.modifiers.barrier=true;

// Bookmarklet:
// javascript:Phaser.GAMES[0].prodigy.player.modifiers.barrier%3D!0%3Bvoid+0
